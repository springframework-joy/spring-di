# Spring DI
      *Setter Based
      *Construction Based
      *Property Based
Annotations
      *@Autowired
      *@Controller
      *@Service
      *@Primary
      *@Qualifier
      @Profiles and 
      @Profile({"en", "default"}) -- When no profile is active we can have and use default one
